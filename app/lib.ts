export type Severity = 'critical' | 'high' | 'medium';
export type OpsAlert = { id:number; status:string; severity:Severity; risk_score:number; confidence_score:number; signal:string; occurrences:number; item_name:string; last_seen_at:string; suppressed:boolean; direction:'UP'|'DOWN'|'MIXED'; min_unit_price:number;max_unit_price:number;min_deviation_pct:number;max_deviation_pct:number;price_deviation_pct:number; baseline_price:number; volume_ratio:number; feature_payload:Record<string,number>;price_clusters:Array<{priceBucket:number;minPrice:number;maxPrice:number;minDeviation:number;maxDeviation:number;occurrences:number}> };
export type Suppression = {id:number;name:string;item_pattern:string|null;starts_at:string;ends_at:string;reason:string;enabled:boolean};
export type AnalysisCase = {id:number;title:string;status:string;priority:string;owner:string|null;summary:string|null;item_name:string;severity:string;risk_score:number};
export type Operations = { summary:{activeAlerts:number;highAlerts:number;collectionHealthy:number;collectionTotal:number;baselineReady:number}; trend:Array<{bucket_at:string;alert_count:number;average_risk:number}>; alerts:OpsAlert[]; topItems:Array<{item_name:string;alerts:number;max_risk:number;occurrences:number}>; collectionHealth:Array<{id:number;item_name:string;last_collected_at:string;last_error:string|null;status:string;listing_count:number;sold_count:number;duration_seconds:number}>; rules:Array<{id:number;name:string;description:string;version:number;enabled:boolean;thresholds:Record<string,number>}>; suppressions:Suppression[]; cases:AnalysisCase[] };
export type MarketRow = { id:string; item:string; category:string; price:string; delta:string; volume:string; score:number; confidence:number; baselineSampleCount:number; baselineReady:boolean; volumeRatio:number; severity:Severity; signal:string; time:string; imageUrl:string };
export type MarketResponse = { source:string; fetchedAt:string; query:string; summary:{listingCount:number;soldSampleCount:number;medianSoldPrice:number;averageListingPrice:number;highRiskCount:number;averageRiskScore:number;watchItemCount:number;baselineReadyItems:number;lastCollectionAt:string|null}; rows:MarketRow[]; limitations:string };
export const apiBase = process.env.NEXT_PUBLIC_MARKET_API_URL || 'http://localhost:4000';
